// Bulk delete failed scheduled actions Woocommerce
add_action('init', function() {
    global $wpdb;
    $wpdb->query("
        DELETE FROM {$wpdb->prefix}actionscheduler_actions
        WHERE status='failed'
    ");
});
